= RELEASES

== Asccidoc
----
asciidoctor -b html5 -r asciidoctor-diagram -D docs *.adoc
----

== Delete the old development branches
=== Local
----
# Delete the old development branches
git branch -d dev-1.1.0
git branch -d dev-1.2.0
git branch -d release-1.0.0
git branch -d release-1.1.0
git branch -d release-1.2.0
git branch -d release-1.2.1
git branch -d p_ajax_update_save_button_on_pivot_ui
git branch -d correct_hitory_view
----

=== Remote
----
# Delete the old dev branches from GitHub
git push origin --delete dev-1.1.0
git push origin --delete dev-1.2.0
git push origin --delete release-1.0.0
git push origin --delete release-1.1.0
git push origin --delete release-1.2.0
git push origin --delete release-1.2.1
git push origin --delete p_ajax_update_save_button_on_pivot_ui
git push origin --delete correct_hitory_view
----

== Delete the wrong tag
----
# Delete it from your local computer
git tag -d v1.0.0

# Delete it from GitHub
git push origin --delete v1.0.0
----

== 1.2.1
----
git tag -a v1.2.1 -m "Release version 1.2.1"
git push origin v1.2.1
git tag
git describe --tags
----


== 1.2.2
----
git tag -a v1.2.2 -m "Release version 1.2.2"
git push origin v1.2.2
git tag
git describe --tags
----


== 1.2.3
----
git tag -a v1.2.3 -m "Release version 1.2.3"
git push origin v1.2.3
git tag
git describe --tags
----


== 1.2.4
----
git tag -a v1.2.4 -m "Release version 1.2.4"
git push origin v1.2.4
git tag
git describe --tags
----


== 1.2.5
----
git tag -a v1.2.5 -m "Release version 1.2.5"
git push origin v1.2.5
git tag
git describe --tags
----

